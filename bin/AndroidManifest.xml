<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.ait.android.travelingsalesman"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="hu.ait.android.travelingsalesman.MainActivity"
            android:label="@string/traveling_salesman" >
        </activity>
        <activity
            android:name="hu.ait.android.travelingsalesman.HomeActivity"
            android:label="@string/traveling_salesman" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="hu.ait.android.travelingsalesman.EnterPointsActivity"
            android:label="@string/traveling_salesman" >
        </activity>
        <activity
            android:name="hu.ait.android.travelingsalesman.TutorialActivity"
            android:label="@string/title_activity_tutorial" >
        </activity>
        <activity
            android:name="hu.ait.android.travelingsalesman.GameActivity"
            android:label="@string/title_activity_game" >
        </activity>
    </application>

</manifest>
